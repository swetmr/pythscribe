@wasm
def response_contrast(x: float) -> float:
    # SAME body as `response`, DEDICATED binding (S-13): the server-side contrast runs THIS one so a
    # native-slider drag reruns the script without inflating any other surface's counters.
    return (x * x) * 0.1 - x + 3.0
