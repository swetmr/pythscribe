# pythscribe @wasm demos — minimal dependencies.
# pythscribe bundles the `pyths` compiler + the wasm runtime (wasmtime is a core dependency); the extras add the demo hosts.
pythscribe[gradio,streamlit]
numpy
pillow
# to run the two notebooks:
jupyterlab
# the wasm_features_demo timing table compares against Numba (optional; skip if unavailable):
numba
