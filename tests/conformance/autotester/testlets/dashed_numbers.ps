# Ported from Transcrypt dashed_numbers/__init__.py (Apache-2.0). Edits:
# - none
# (AutoTester shim is inlined by the runner at execution time.)

import math

def run (autoTester):
    autoTester.check (1000_000_000)
    autoTester.check (1_200.000_3e2)
    autoTester.check (0x_ff_ff_ff)
