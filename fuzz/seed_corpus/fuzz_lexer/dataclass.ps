from dataclasses import dataclass

@dataclass
class Point:
    x: int
    y: int

@dataclass
class User:
    name: str
    age: int = 0

    def greet(self) -> str:
        return f"Hi, I'm {self.name}"

p = Point(1, 2)
u = User("Alice", 30)
print(u.greet())
